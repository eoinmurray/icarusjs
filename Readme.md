git subtree push --prefix dist origin gh-pages

TODO:

- hh+vv:
    + both [+]
    + qwp only [+]
    + hwp only [+]

- hh-vv:
    + both
    + qwp only
    + hwp only

- hv+vh:
    + both
    + qwp only
    + hwp only

- hv-vh:
    + both
    + qwp only
    + hwp only

